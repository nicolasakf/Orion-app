/** Local storage key for recently opened Jupyter workspace folders. */
export const RECENT_PROJECTS_STORAGE_KEY = "recentProjects";

/** Max recent projects kept in storage and shown in project shortcuts. */
export const MAX_RECENT_PROJECT_PATHS = 10;
