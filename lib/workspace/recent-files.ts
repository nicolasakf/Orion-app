/** Local storage key for recently opened Jupyter files. */
export const RECENT_FILES_STORAGE_KEY = "recentFiles";

/** Max recent files kept in storage and shown in file shortcuts. */
export const MAX_RECENT_FILE_PATHS = 10;
