/** Default number of visible rows in the table */
export const VISIBLE_ROW_COUNT = 15;

/** Default row height in pixels */
export const ROW_HEIGHT = 40;

/** Default font size in pixels */
export const FONT_SIZE = 14;

/** Minimum column width in pixels */
export const MIN_COLUMN_WIDTH = 50;

/** Default column width in pixels (used when no explicit width is set) */
export const DEFAULT_COLUMN_WIDTH = 150;

/** Delay in ms before focusing filter input after opening */
export const FILTER_FOCUS_DELAY_MS = 100;

/** Minimum allowed font size in pixels */
export const MIN_FONT_SIZE = 10;

/** Maximum allowed font size in pixels */
export const MAX_FONT_SIZE = 24;

/** Minimum allowed row height in pixels */
export const MIN_ROW_HEIGHT = 20;

/** Maximum allowed row height in pixels */
export const MAX_ROW_HEIGHT = 100;

/** Minimum allowed visible row count */
export const MIN_VISIBLE_ROWS = 5;

/** Maximum allowed visible row count */
export const MAX_VISIBLE_ROWS = 100;
