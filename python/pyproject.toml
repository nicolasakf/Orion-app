[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "orion-notebook"
version = "0.9.1"
description = "Local Orion CLI launcher for notebooks and Jupyter"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "Apache-2.0" }
authors = [{ name = "Nicolas Fonteyne" }]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/nicolasakf/Orion-app"
Repository = "https://github.com/nicolasakf/Orion-app"
Issues = "https://github.com/nicolasakf/Orion-app/issues"

[project.scripts]
orion = "orion_agent.cli:main"

[tool.setuptools]
packages = ["orion_agent"]
